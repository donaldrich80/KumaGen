# Server port
PORT=3001

# Anthropic API Key (can also be set via the Settings UI)
ANTHROPIC_API_KEY=

# Uptime Kuma URL (can also be set via the Settings UI)
KUMA_URL=http://localhost:3001

# Uptime Kuma credentials (can also be set via the Settings UI)
KUMA_USERNAME=
KUMA_PASSWORD=
